# ============================================================
# TrustField - requirements.txt
# Python 3.10+  |  Ghousia College of Engineering 2025-2026
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
httpx==0.27.0

# ── Cloud SDKs ───────────────────────────────────────────────
boto3==1.34.100                  # AWS IAM + CloudTrail
botocore==1.34.100
azure-identity==1.16.0           # Azure authentication
azure-mgmt-authorization==4.0.0  # Azure IAM/RBAC
azure-mgmt-monitor==6.0.1        # Azure Activity Logs
google-cloud-iam==2.15.1         # GCP IAM
google-cloud-logging==3.10.0     # GCP Audit Logs
google-auth==2.29.0

# ── Kubernetes ───────────────────────────────────────────────
kubernetes==29.0.0               # K8s RBAC collection

# ── Graph Database ───────────────────────────────────────────
neo4j==5.20.0                    # Neo4j Python driver
py2neo==2021.2.4                 # OGM for Neo4j (optional helper)

# ── Graph Analysis ───────────────────────────────────────────
networkx==3.3                    # BFS/DFS traversal algorithms

# ── Machine Learning ─────────────────────────────────────────
scikit-learn==1.4.2              # Isolation Forest, preprocessing
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2                    # Model serialisation

# ── Deep Learning / GNN ──────────────────────────────────────
torch==2.3.0                     # PyTorch (CPU build)
torch-geometric==2.5.3           # Graph Neural Networks (GAT)
torch-scatter==2.1.2
torch-sparse==0.6.18

# ── Database (PostgreSQL) ────────────────────────────────────
sqlalchemy==2.0.30               # ORM
psycopg2-binary==2.9.9           # PostgreSQL driver
alembic==1.13.1                  # DB migrations

# ── Database (MongoDB - audit logs) ──────────────────────────
pymongo==4.7.2
motor==3.4.0                     # Async MongoDB driver

# ── API Utilities ────────────────────────────────────────────
pydantic==2.7.1                  # Data validation
pydantic-settings==2.2.1         # Settings management
python-jose[cryptography]==3.3.0 # JWT auth
passlib[bcrypt]==1.7.4           # Password hashing

# ── Notifications / Alerting ─────────────────────────────────
slack-sdk==3.27.1                # Slack alerts
boto3                            # SNS alerts (already listed above)

# ── Logging & Monitoring ─────────────────────────────────────
loguru==0.7.2                    # Structured logging
opentelemetry-sdk==1.24.0        # Tracing

# ── Testing ──────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
moto[iam,sts,ec2]==5.0.7         # AWS mock for testing

# ── Dev Tools ────────────────────────────────────────────────
python-dotenv==1.0.1             # .env loader
black==24.4.2                    # Code formatter
flake8==7.0.0                    # Linter
mypy==1.10.0                     # Type checker
pre-commit==3.7.1                # Git hooks

# ── Data Utilities ───────────────────────────────────────────
python-dateutil==2.9.0
tqdm==4.66.4                     # Progress bars for scans
rich==13.7.1                     # Pretty CLI output